---
name: kblens-kb
description: "Search a pre-built code knowledge base generated by KBLens to answer questions about code architecture, type or symbol definitions, public API signatures, package contents, component responsibilities, usage sites, and cross-module or cross-package dependencies. ALWAYS trigger this skill when the user asks about any class, struct, interface, type, function, method, symbol, component, package, module, subsystem, or system in the codebase, including prompts like 'what does X do?', 'how does Y work?', 'where is Z defined?', 'show me the public API of...', 'which components depend on X?', 'explain this module or class', 'what is the architecture of...', 'how do these modules interact', 'list components in this package', 'what components are in package X', 'compare component X and component Y across packages', 'where is this class or component used', and 'which files or modules reference X'. Also trigger when the task requires understanding existing code structure first, such as refactoring, adding features to a component, debugging cross-module issues, browsing a package, or comparing systems across sources. Do NOT trigger for general programming concepts or KBLens tool configuration. Resolve the knowledge base location from the user's active KBLens configuration instead of assuming a fixed path."
---

# KBLens Code Knowledge Base Query Skill

## Goal

Let the agent answer code architecture questions by searching a KBLens-generated hierarchical Markdown knowledge base, rather than guessing or reading every source file. The knowledge base is generated from language-aware code analysis and covers component responsibilities, key types or symbols, public interfaces, and dependencies.

## Step 0: Locate the Knowledge Base

The knowledge base location is not hardcoded. Resolve it from the active KBLens configuration:

1. Read the user's KBLens global configuration file.
2. Get the `output_dir` field.
3. List subdirectories under `output_dir` - each subdirectory is a **source**.

Each source has this internal structure:
```
<source_name>/
├── INDEX.md              # L0: global index listing all packages with links
├── _meta.json            # Metadata (component status, token stats)
└── <source_name>/        # packages directory (same name as source)
    ├── package_a.md      # L1: package overview (component grouping, deps, navigation)
    ├── package_a/        # L2: components within the package
    │   ├── ComponentA.md           # Component overview
    │   ├── ComponentA/             # Leaf batch details (large components only)
    │   │   ├── src_component_a.md
    │   │   └── ...
    │   └── PackageA_component.interface.md  # Small component (single file)
    ├── package_b.md
    ├── package_b/
    │   ├── ComponentB.md
    │   └── ComponentB/
    └── ...
```

Three-layer hierarchy:
- **L0 (INDEX.md)**: Source-level overview + package directory (with hyperlinks)
- **L1 (<package>.md)**: Component grouping within package, cross-component deps, navigation guide
- **L2 (<package>/<component>.md)**: Component overview — Purpose, Architecture, Key API names, Dependencies. For large components, a subdirectory contains leaf files with full API signatures; for small components, signatures are inline in this file.

## Leaf File Structure

Leaf-level `.md` files in component subdirectories, such as `ComponentA/src_component_a.md`, have two sections separated by `---`:

1. **Summary section** (above `---`): LLM-generated overview — Responsibility, Key Types, Source Files, Dependencies. Compact and high-level.
2. **`## Complete API Signatures`** (below `---`): Raw extracted signatures and symbol definitions - for example function or method declarations, class or type definitions, and other language-level API details. This is the authoritative source for exact signatures.

**Reading strategy to minimize context usage**:
- For architecture questions: read only the summary section (above `---`)
- For specific function signatures: grep the file, then use Read with offset/limit around the match line
- Do NOT read the entire leaf file into context if you only need the summary — the AST section can be very large

## Step 1: Determine Search Type

Based on the user's question, choose a search path:

| Question pattern | Search type | Example |
|-----------------|-------------|---------|
| Contains a specific class, struct, interface, type, method, or function name | **Exact search** | "What is `ComponentState`?" |
| Asks about a system or module architecture | **Semantic search** | "How does the input system work?" |
| Asks about a package or project overview | **Browse search** | "What is in the rendering package?" |
| Asks about cross-component relationships | **Relationship search** | "How do the audio and perception systems interact?" |

## Step 2a: Exact Search (symbol or type name)

When the user gives a specific identifier:

1. **Grep search** across the knowledge base root (`output_dir`), covering all sources:
   ```
   Grep "<symbol_name>" in <output_dir>/ (include *.md)
   ```

2. **Rank results** by relevance:
    - **Highest**: Interface, declaration, or API-focused files - typically where the symbol or component is **defined**
    - **High**: Core or implementation-oriented files - typically implementation logic or detailed behavior
    - **Medium**: Same-named component overview `.md` (for example `Input.md`) - provides inter-component context
    - **Lowest**: Other files referencing the symbol - usually consumers, not the definition

3. **Read the most relevant 1-2 files** to answer the question.

4. **Fallback when not found**: If Grep finds nothing in the knowledge base, the symbol may not have been extracted. Then:
    - Try searching a **related component name** (for example, searching `UpdateState` fails -> try `State` -> find `State.core.md`)
   - Get **source file paths** from `_meta.json` (`path` field records absolute path for each component)
   - Use those paths to search actual source code

## Step 2b: Semantic Search (system/architecture)

When the user asks how a system works:

1. **Read INDEX.md** to identify relevant packages and sources
2. **Read L1 package .md** to locate specific components, get grouping and dependency info
3. **Read L2 component .md** for detailed architecture, types, API
4. For finer-grained info, check if the component has a **subdirectory** (large components only) and read leaf `.md` files

## Step 2c: Browse Search (package/project overview)

- What's in the project → Read INDEX.md
- What's in a package -> Read the corresponding L1 `.md`
- Unsure which source → Read each source's INDEX.md in turn

## Step 2d: Relationship Search (cross-component dependencies)

1. Read relevant package L1 .md — look for "Cross-Component Dependencies" section
2. Read relevant component L2 .md — look for "Dependencies" section
3. For cross-source relationships, search in both sources

## Filename Mapping Rules

Knowledge base `.md` filenames are generated from the component `name` field by replacing `/` with `_`:
- `PackageA/component.interface` -> `PackageA_component.interface.md`
- `Subsystem/module.core` -> `Subsystem_module.core.md`
- `ComponentA` -> `ComponentA.md`

## Limitations

- **Public API and extracted structure only**: The knowledge base is based on code analysis output. It is strongest for declarations, interfaces, structure, relationships, and extracted signatures, but may not include every implementation detail or function body.
- **Dependencies may be incomplete**: If a `.md` says "No explicit dependencies visible in AST excerpt.", it just means no `#include` was seen in the extracted AST, not that there are truly no dependencies
- **For implementation details**: Use the summary section to understand architecture and the `## Complete API Signatures` section for exact signatures. For actual implementation logic (function bodies), search source code directly
- **Language coverage depends on the generated knowledge base**: Use the knowledge base when the user's question is about code represented in the generated KBLens output.
